# =============================================================================
# JunkOS Backend - Environment Variables
# =============================================================================
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#
# IMPORTANT: Never commit .env to version control.
# =============================================================================

# ---------------------------------------------------------------------------
# Flask
# ---------------------------------------------------------------------------
# Set to "production" in deployed environments
FLASK_ENV=development

# Flask secret key for session signing. Generate with:
#   python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=

# ---------------------------------------------------------------------------
# Database
# ---------------------------------------------------------------------------
# PostgreSQL connection URL (set automatically by Railway/Render).
# Leave blank to use local SQLite (junkos.db).
# DATABASE_URL=postgresql://user:password@host:5432/junkos

# Legacy SQLite database path (only used when DATABASE_URL is not set)
DATABASE_PATH=junkos.db

# ---------------------------------------------------------------------------
# Authentication
# ---------------------------------------------------------------------------
# JWT secret for token signing. Generate with:
#   python -c "import secrets; print(secrets.token_hex(32))"
# MUST be set in production — otherwise tokens will not survive restarts.
JWT_SECRET=

# API key for legacy endpoints (X-API-Key header).
# Generate with:
#   python -c "import secrets; print(secrets.token_urlsafe(32))"
API_KEY=

# Secret required by POST /api/auth/seed-admin to promote a user to admin.
# Must be set before you can seed your first admin user.
ADMIN_SEED_SECRET=

# ---------------------------------------------------------------------------
# CORS
# ---------------------------------------------------------------------------
# Comma-separated list of allowed origins. Use * for development.
# Example: https://platform.example.com,https://landing.example.com
CORS_ORIGINS=*

# ---------------------------------------------------------------------------
# Stripe Payments
# ---------------------------------------------------------------------------
# Stripe secret key (starts with sk_test_ or sk_live_)
STRIPE_SECRET_KEY=

# Stripe webhook signing secret (starts with whsec_)
STRIPE_WEBHOOK_SECRET=

# ---------------------------------------------------------------------------
# Twilio SMS
# ---------------------------------------------------------------------------
# Leave all three blank to disable SMS (messages will be logged to console).
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_FROM_NUMBER=

# ---------------------------------------------------------------------------
# Email - Resend (preferred)
# ---------------------------------------------------------------------------
# Resend API key (starts with re_). If set, emails are sent via Resend.
# Sign up at https://resend.com
RESEND_API_KEY=

# ---------------------------------------------------------------------------
# Email - SendGrid (legacy fallback)
# ---------------------------------------------------------------------------
# SendGrid API key. Used only if RESEND_API_KEY is not set.
SENDGRID_API_KEY=

# ---------------------------------------------------------------------------
# Email - Shared settings
# ---------------------------------------------------------------------------
# "From" email address and display name for outgoing emails.
EMAIL_FROM=bookings@junkos.com
EMAIL_FROM_NAME=JunkOS

# ---------------------------------------------------------------------------
# Pricing
# ---------------------------------------------------------------------------
# Base price (in dollars) added to every booking
BASE_PRICE=50.0

# ---------------------------------------------------------------------------
# Server
# ---------------------------------------------------------------------------
# Port the Flask server listens on (set automatically by Railway/Render).
PORT=8080
